spring:
  datasource:
    url: jdbc:mysql://localhost:3306/dev_db # 기본 dev 설정 (개발자별 secret 파일에서 덮어씀)
    username: dev_user
    password: dev_password
  jpa:
    hibernate:
      ddl-auto: update # 개발 환경에서는 업데이트 허용
  logging:
    level:
      root: DEBUG
